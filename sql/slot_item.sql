CREATE TABLE IF NOT EXISTS slot_item (
    api_id INTEGER PRIMARY KEY,
    api_slotitem_id INTEGER,
    api_locked INTEGER,
    api_level INTEGER,
    api_alv INTEGER,
    api_used_ship INTEGER,
    api_used_air_base INTEGER
);