CREATE TABLE IF NOT EXISTS mst_furniture (
    api_id INTEGER PRIMARY KEY,
    api_type INTEGER,
    api_no INTEGER,
    api_title TEXT,
    api_description TEXT,
    api_rarity INTEGER,
    api_price INTEGER,
    api_saleflg INTEGER,
    api_bgm_id INTEGER,
    api_version INTEGER,
    api_outside_id INTEGER,
    api_active_flag INTEGER
);