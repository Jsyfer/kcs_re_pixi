CREATE TABLE IF NOT EXISTS material (
    id INTEGER PRIMARY KEY,
    fuel INTEGER,
    bull INTEGER,
    steel INTEGER,
    aluminium INTEGER,
    construction INTEGER,
    repair INTEGER,
    development INTEGER,
    renovation INTEGER,
);